import React from 'react';
import axios from 'axios';

const Trip = () => (
    <p>Trip stuff goes here, 
    call it in programatically instead of manually
    the way we have it set up now conflicts,
    current logical way makes it so that the trip list shows up
    where it's called. 
    </p>
);

//

export default Trip;